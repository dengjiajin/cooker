# 关键发现

## 项目定位

- 这是一个长期项目，不适合只靠聊天上下文推进，必须依赖 `.codex-memory/` 和计划文件持久化上下文。
- 第一期不是完整业务交付，而是平台底座版。

## 核心业务结论

- 所有人必须先通过客户端微信登录并完成手机号授权，才能成为平台用户。
- 用户登录后立即进入后管用户列表。
- 厨师是用户扩展出的身份，审批通过后进入后管厨师列表。
- 厨师端登录手机号必须与用户手机号一致。
- 后管只有管理员可登录。
- 后管采用完整 `RBAC + sa-token`。

## 数据与状态结论

- 用户、厨师、管理员都需要启用/禁用状态字段。
- 申请表需要审核流程状态字段。
- `creator/create_time/updater/update_time` 是项目默认审计字段。
- 逻辑删除字段是否引入，按表逐个确认。

## 技术栈结论

- 后端：`Spring Boot 3 + MyBatis-Plus + MySQL + Redis + Sa-Token`
- 后管 Web：`Vue 3 + Vite + TypeScript + Element Plus + Pinia`
- 小程序：原生微信小程序

